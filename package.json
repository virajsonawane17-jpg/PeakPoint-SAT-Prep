{
  "name": "peakpoint-sat-prep",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "PeakPoint SAT Prep with secure Gemini-powered learning features.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
